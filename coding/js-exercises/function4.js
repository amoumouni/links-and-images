function lastLetter(str) {
 let lastLetter = str.length - 1;
 return str[str.length - 1];
}

//test examples
console.log(lastLetter("ari")) // should print i
console.log(lastLetter("hello")) // should print o

